Tested PDF Password lock Tool - pdf file uploaded then password added and lock pdf button was clicked.However it did not lock the pdf file. And Flashora splash screen or app loading screen is visible instead of showing result.It might crashed.
Tested compressed pdf tool - required pdf file uploaded then compression level is selected and clicked compress pdf button however it doesn't compressed the file. Splash screen is visible instead of result page.
Also compression levels are not working properly or as expected. High compression should compress more and low compression should compress less. But it doesn't work like that.


