diff --git a/node_modules/nativewind/dist/metro/tailwind-cli.js b/node_modules/nativewind/dist/metro/tailwind-cli.js
index 77fba4c..3c31e68 100644
--- a/node_modules/nativewind/dist/metro/tailwind-cli.js
+++ b/node_modules/nativewind/dist/metro/tailwind-cli.js
@@ -69,6 +69,14 @@ async function tailwindCli(input, metroConfig, options) {
             windowsHide: true,
         });
         cli.on("error", (error) => reject());
+        cli.on("close", (code) => {
+            clearTimeout(timeout);
+            if (code === 0) {
+                done(nativewindOptions);
+            } else {
+                reject();
+            }
+        });
         cli.stderr.on("data", (data) => {
             data = data.toString();
             if (data.includes("tailwindcss/lib/cli") || data.includes("npm ERR!")) {
