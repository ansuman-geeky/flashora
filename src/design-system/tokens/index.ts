/**
 * Flashora Design System — Token Barrel Export
 *
 * Import all design tokens from this single entry point:
 *   import { Colors, Typography, Spacing, Radius, Shadow, Motion } from '@design-system/tokens';
 */

export { Colors } from './colors';
export type { ColorToken } from './colors';

export { Typography } from './typography';
export type { FontSize, FontFamily, LineHeight } from './typography';

export { Spacing, Radius, Shadow, Motion } from './spacing';
export type { SpacingToken, RadiusToken, ShadowToken } from './spacing';
