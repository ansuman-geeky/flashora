/**
 * src/features/scanner/constants/scannerLayout.ts
 * Extracted from flashora_scanner_screens.html and redesign spec.
 */

export const ScannerLayout = {
  screenPadding:    18,   // horizontal padding on all screens
  cardRadius:       12,   // border-radius for format cards
  pillRadius:       20,   // border-radius for mode pills
  buttonRadius:     12,   // primary action buttons
  cornerDotSize:    16,   // draggable corner handle diameter
  pageThumbW:       52,   // page thumbnail width (export screen)
  pageThumbH:       68,   // page thumbnail height (export screen)
  cameraThumbW:     40,   // page thumbnail width (camera screen)
  cameraThumbH:     52,   // page thumbnail height (camera screen)
  pageThumbRadius:  8,
  captureButtonOuter: 64,
  captureButtonInner: 50,
  headerHeight:     48,
  sliderTrackH:     4,
  sliderThumbSize:  14,
  formatIconSize:   36,   // format card icon box
  formatIconRadius: 8,
};
